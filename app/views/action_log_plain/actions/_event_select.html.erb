<%#= select_tag "aktion[event_id]", create_user_select_tag_options(aktion.nil? ? nil : aktion.event, Event.find_all_by_event_area_id(event_area_id), true), :id => :aktion_event_id, :style=>"width:300px;" %>
<%= collection_select(:aktion, :event_id, Event.find_all_by_event_area_id(event_area_id), :id, :name, { :include_blank => true }, :style=>"width:150px;") %>
